# secrets/

**只放 `.example` 模板**，不放任何真值。

## 使用方式

```bash
# 项目根目录
cp shared/secrets/obe-api.example.env .env.local
# 编辑 .env.local 填入真实值
```

`.env.local` 已在根 `.gitignore` 中。

## 已有模板

（暂无，按需添加）

## 命名

`{用途}.example.env` — 例：`obe-api.example.env`、`figma.example.env`

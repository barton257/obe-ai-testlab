# 昨日遗留问题和任务汇总 — 2026-08-17 → 2026-08-18

> 基于 `2026-08-17-TODO.md` + GPT E2E 审查结果

---

## 一、核心遗留问题（P0 级）

### 1. ⚠️ Playwright 并发跑登录态失效 ← **最大技术债**

**现象**：
- 单跑任何 spec（subscribe-happy / risk-disclosure / i18n / logout）全绿
- 全量跑时，setup 之后 storageState 加载偶发失效，页面弹 auth-modal
- test-results 显示 6 条失败（空金额/0/0.5/happy/i18n/risk-disclosure）

**已排除**：
- ❌ session 单点顶掉（你没登录过 barton.c）
- ❌ storageState 文件损坏
- ❌ token 长度异常

**待验证**：
- 投入 API 调用（invest/purchase）后前端触发 logout
- JSESSIONID 被 server revoke
- storageState 加载时机问题（dependencies 配置）

**破局选项**：
- A. 每个 spec 的 `beforeEach` 里调 `loginViaUI` 重新登录（慢 6s × 用例数，绝对稳）
- B. `beforeEach` 探测 `.auth-modal-overlay` 存在时自动重登（兼顾速度）
- C. 等 CI 环境跑，排查是不是本机代理导致

**关联文件**：
- `auth.setup.ts`
- `playwright.config.ts`
- `.auth/user1.json`

---

### 2. ⚠️ E2E 测试用例审查发现的问题（GPT Review）

**P0-1：反向断言缺陷行为** — ✅ **已修复（2026-08-18 上午）**
- 原问题：空金额/0/0.5 三个用例用 `not.toBeDisabled()`，断言按钮可点击（错误行为）
- 修复：改用正向断言 `toBeDisabled()` + `test.fail()` 标记
- 实测：单跑 `subscribe-frontend-validation` → `6 passed`（3 条 expected-failure 正确计入）
- 影响：缺陷修复后移除标记即转为正式回归用例

**顺带查实的关键线索**：GPT 审查称「已有失败报告显示 0 和 0.5 时按钮实际已禁用，
测试假设已过期」。单跑复测结论相反 —— 按钮是 `enabled`，**原缺陷记录成立，审查此条不成立**。
昨天产物里的 `disabled` 来自 storageState 失效后的未登录态。
即 **P0-1（登录态失效）不只让测试变红，还会让断言结论反转**，排查优先级应再提一档。

**P0-2：Happy Path 断言不足** — ⏳ **未修复**
- 问题：只检查 Kakarotto heading 存在，历史已有订阅时会误通过
- 阻塞：需要"我的订阅"列表的 DOM 结构，但唯一留档的 trace 在步骤 2 就超时，没跑到该页
- 已做：在 `subscribe-happy.spec.ts` 留 TODO 说明断言缺口和校准方式
- 下一步：`npm run codegen` 走到"我的订阅"，抓记录行/金额/时间的语义定位

**P0-3：资金类测试无幂等性** — ⚠️ **待解决（P1 级）**
- 问题：每次跑 happy path 都真实扣款订阅
- 影响：测试账户余额不断减少，无法重复运行
- 待定方案：专用测试账户 + 数据清理脚本 / 赎回还原

**P0-4：认证方案文档不一致** — ✅ **已修复（2026-08-18 上午）**
- 原问题：README.md 描述 JWT fixture，实际代码用 storageState
- 修复：README.md 认证策略章节改为 `auth.setup.ts` + storageState 方案
- 影响：文档与代码一致

---

### 3. ⚠️ API 回归用例缺失（对应已发现 bug）

需要补齐 7 条用例：

| 用例 | 对应 Bug | 断言 | 标记 |
|---|---|---|---|
| `test_purchase_below_min` | S2 订阅金额穿透 | 400 拒绝 | `xfail` |
| `test_purchase_equal_min` | — | 200 通过 | — |
| `test_purchase_equal_max` | — | 200 通过 | — |
| `test_purchase_above_max` | — | 400 拒绝 | — |
| `test_purchase_bot_paused` | — | 400 拒绝 | — |
| `test_redeem_exceeds_equity_rejected` | S2 赎回静默截断 | 400 拒绝 | `xfail` |
| `test_purchase_wrong_user_id` | S3 userId 不匹配 | 403 | `xfail` |

**依赖**：等后端修复 S2/S3 bug

---

### 4. ⚠️ TEST_STRATEGY 5 条 TODO 待评审

见 `SPARTANS_SUBSCRIBE_TEST_STRATEGY.md` 末尾：

| # | 问题 | 需要 | 优先级 |
|---|---|---|---|
| 1 | 批次窗口内多次订阅合并 or 分记录？ | 产品 | P0 |
| 2 | Tier 参数更改通知机制？ | 产品 | P0 |
| 3 | metric/trace 命名规范（和 SRE 对齐） | 后端 | P0 |
| 4 | Private bot 白名单管理入口 | 产品 | P0 |
| 5 | 批次失败时的 Kafka topic / DB 状态 | 后端 | P0 |

**阻塞**：影响测试策略完整性，无法补全用例

---

### 5. ⚠️ 缺失的规范文档

| 文件 | 用途 | 优先级 |
|---|---|---|
| `shared/conventions/priority.md` | P0–P3 判定标准 | P0 |
| `shared/conventions/testcase-naming.md` | 用例命名规则 | P1 |
| `shared/conventions/commit-message.md` | commit 规范 | P1 |
| `ai-testlab/templates/testcase.yaml` | 用例模板骨架 | P1 |
| `ai-testlab/templates/bug.md` | 缺陷模板骨架 | P1 |

---

## 二、昨日已完成（2026-08-17）✅

### 基础设施

- ✅ API 三层接口栈（obe_http.py + spartans client + SPARTANS_API.md）
- ✅ 9 条 API smoke 全绿
- ✅ 登录换 token 脚本（`scripts/spartans_login.py`）
- ✅ Playwright 骨架（18 条测试，storageState 方案，auth.setup.ts）
- ✅ fixtures/tier_config.yaml（Tier 参数 + 批次窗口基线）

### 缺陷发现与分级

- ✅ S1 验证脚本（`scripts/verify_bug_s1.py`）
- ✅ S1 降级为 S2（赎回静默截断，非越权）
- ✅ S3 越权定性（userId 与 JWT 不匹配，降级为设计缺陷）
- ✅ 3 份 bug 报告（S2 订阅穿透 / S2 赎回截断 / S3 userId 不匹配）
- ✅ bug-severity.md（S1–S4 分级标准 + 判定树 + SLA）

### 策略与文档

- ✅ dod.md（Definition of Done 上线闸）
- ✅ TEST_STRATEGY 模板 + test-strategy-generator skill
- ✅ SPARTANS_SUBSCRIBE_TEST_STRATEGY.md（首个完整策略示例）
- ✅ Roadmap P0–P2（`2026-08-17-p0-p2.md`）

### 环境配置

- ✅ USER2/USER3 token 已写入 .env.local

---

## 三、今日已完成（2026-08-18 上午）✅

| # | 任务 | 状态 |
|---|---|---|
| 1 | 金额校验 3 个缺陷用例改用 `test.fail()` 标记 | ✅ 完成并实测 |
| 2 | Happy Path 增强断言（金额+时间戳验证） | ⏳ 未完成，DOM 未校准 |
| 3 | README.md 认证方案文档统一 | ✅ |
| 4 | 更新审查文档 + 勘误 1 条 | ✅ |
| 5 | 输出今日计划表 | ✅ |

**成果文件**：
- `subscribe-frontend-validation.spec.ts` — 正向断言 + test.fail()，头部注释记录登录态干扰
- `subscribe-happy.spec.ts` — 留 TODO 说明断言缺口（未加猜测选择器）
- `README.md` — 认证策略章节更新为 storageState 方案
- `SPARTANS_E2E_TEST_CASE_REVIEW_20260818.md` — 标注修复状态 + §3.4 勘误
- `2026-08-18-今日计划.md` — 今日任务清单

---

## 四、今日待做（2026-08-18 下午）⏳

### P0 优先级（阻塞上线）

| # | 任务 | 预计耗时 | 依赖 |
|---|---|---|---|
| 1 | Playwright 并发登录态失效排查 + 修复 | 2-3h | 无 |
| 2 | API 回归用例补齐（7 条） | 1h | 后端修复 S2 |
| 3 | 跑一次完整 API E2E（22 分钟） | 30min | 无 |
| 4 | 评审 TEST_STRATEGY 5 条 TODO | 1h | 产品+后端 |
| 5 | 补 `shared/conventions/priority.md` | 10min | 无 |

---

## 五、本周剩余任务（2026-08-19 起）

### P0 剩余

- 后端修复 S2 订阅金额穿透 + QA 回归

### P1（本迭代 2026-08-18 → 2026-09-07）

| # | 任务 | 分类 |
|---|---|---|
| P1-1 | 后端修复 S3 userId 与 JWT 不匹配 + QA 回归 | Bug |
| P1-2 | 数据一致性对账测试（五路） | 测试 |
| P1-3 | 补 UI E2E（frontend-validation 已有，剩余其他） | 测试 |
| P1-4 | 契约测试骨架 | 测试 |
| P1-5 ~ P1-7 | 赎回/分润/取消订阅 TEST_STRATEGY | 策略 |
| P1-8 | Runbook | 文档 |
| P1-9 | 安全清单 7 项 | 测试 |
| P1-10 ~ P1-14 | 补规范/模板/CI | 基建 |

---

## 六、关键风险与阻塞

| 风险 | 影响 | 应对 |
|---|---|---|
| Playwright 并发问题本地无法复现 | P0-1 无法修复 | 等 CI 环境或先用方案 A |
| 后端 S2 bug 修复延期 | API 回归用例无法验证 | 先写 xfail 标记，修复后移除 |
| 产品/后端约不上时间评审 TODO | 策略文档不完整 | 先记录待确认，不阻塞其他任务 |
| 资金类测试重复运行耗尽余额 | E2E 无法持续跑 | P1 优先做数据隔离方案 |

---

## 七、追踪的关联缺陷

**2026-08-18 已复验：三条全部仍存在，无一修复。** 详细结论写入各 bug 文件的「复验记录」章节。

| Bug | 级别 | 复验状态 | 关联任务 |
|---|---|---|---|
| [订阅金额 < min 穿透](../bugs/2026-08-17-spartans-S2-订阅金额小于最小值未拦截.md) | S2 | 仍存在，`1e-08` 可穿透且**卡成 Locked 永久悬挂** | P0-3 |
| [赎回超权益静默截断](../bugs/2026-08-17-spartans-S2-赎回金额超权益静默截断.md) | S2 | 仍存在，新发现只拦下界不拦上界，修复成本低 | — |
| [userId 与 JWT 不匹配](../bugs/2026-08-17-spartans-S3-userId与JWT不匹配未拦截.md) | S3 | 仍存在，归属二次确认不越权，级别维持 | P1-1 |

三条同属"入参越界后静默处理"根因，建议一并修复。台账见 [`bugs/README.md`](../bugs/README.md)。

---

## 八、今日进度总结

审查文档提的 4 项 P0：1 已修复、4 已修复、2 未完成（DOM 未校准）、3 推 P1（资金隔离）。

**关键进展**：
- ✅ 金额校验用例断言方向修正，实测通过
- ✅ 认证方案文档与代码对齐
- ⏳ Happy Path 断言仍偏弱，需 codegen 校准后补
- ⚠️ P0-1 登录态失效升级为最高优先级：它会让断言结论反转，不止是变红
- ⚠️ API 回归用例依赖后端修复 S2

---

## 九、需要你提供的信息

为了推进下午的任务，我需要：

### 立即需要

1. **Playwright 并发问题排查**：
   - 你是否能复现全量跑失败？（`npx playwright test` 全跑一次）
   - 失败时 `.auth/user1.json` 文件内容是否正常？
   - 你倾向哪个方案（A/B/C）？

2. **后端 S2 bug 修复进度**：
   - 订阅金额穿透 bug 预计何时修复？
   - 需要我先写 xfail 用例，还是等修复后再补？

3. **TEST_STRATEGY 评审时间**：
   - 产品/后端今天有空评审 5 条 TODO 吗？
   - 如果约不上，我先记录「待确认」继续其他任务？

### 可选提供

4. **资金类测试数据隔离**（P1 级）：
   - 是否有专用测试账户？
   - 是否允许每次跑完后调赎回 API 清理数据？

---

**下一步行动**：等你确认上述信息后，我立即开始下午的 P0 任务排查。

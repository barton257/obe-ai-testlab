# SPARTANS 今日计划 — 2026-08-18

> 基于昨天遗留的 [2026-08-17-TODO.md](2026-08-17-TODO.md) 和 GPT 审查结果

---

## ✅ 上午已完成（09:00-12:00）

### 1. E2E 测试 P0 修复（基于 GPT 审查）

| # | 任务 | 状态 |
|---|---|---|
| 1 | 金额校验 3 个缺陷用例改用 `test.fail()` 标记 | ✅ 完成并实测 |
| 2 | Happy Path 增强断言（金额+时间戳验证） | ⏳ **未完成**，见下 |
| 3 | README.md 认证方案文档统一 | ✅ |
| 4 | 更新审查文档标注修复状态 + 勘误 1 条 | ✅ |

**成果**：
- `subscribe-frontend-validation.spec.ts` 三处改为正向断言 `toBeDisabled()` + `test.fail()` 标记，
  单跑实测 `6 passed`（3 条 expected-failure 正确计入通过）
- `README.md` 认证策略章节与实际代码一致（storageState 方案）
- 测试仍可正常收集：18 条

**未完成项（2）**：Happy Path 的金额/时间戳断言需要"我的订阅"列表的 DOM 结构，
而唯一留档的 trace 在步骤 2（点订阅按钮）就超时了，根本没跑到那一页。
先写猜测选择器等于制造假绿，已改为在 spec 里留 TODO，转到下午用 codegen 校准。

**顺带查实一条线索**：昨天的失败产物显示金额校验用例里按钮是 `disabled`，
GPT 审查据此判断"缺陷已修复、测试假设过期"。单跑复测的结论相反 —— 按钮是 `enabled`，
缺陷仍在。昨天那个 `disabled` 是 storageState 失效后的未登录态造成的，
属于 P0-1 的副作用。**这说明 P0-1 不只是让测试变红，还会让断言结论反转**，
下午排查时优先级应再提一档。

---

## 🔄 下午计划（13:00-18:00）

### 2. Playwright 并发登录态失效问题排查（P0-1，昨日最大技术债）

**现象回顾**：
- 单跑任何 spec 都稳定通过
- 全量跑时部分 spec 出现 auth-modal 弹窗
- setup 生成的 `.auth/user1.json` 偶发失效

**排查步骤**：

| # | 步骤 | 预期产出 |
|---|---|---|
| 1 | 单跑 setup + happy path 连续 3 次 | 全绿基线 |
| 2 | 全量跑（18 条）观察失败模式 | 确认哪些 spec 触发失效 |
| 3 | 检查 `.auth/user1.json` 文件内容 | cookie 有效期、domain、path |
| 4 | 对比单跑和全量跑的 network trace | 是否有 logout / session revoke 请求 |
| 5 | 尝试方案 B：`beforeEach` 探测 auth-modal 自动重登 | 绿色率提升 |

**决策点**：
- 如果探测到是 API 调用触发 logout → 改用每个 spec 独立登录（方案 A）
- 如果是 storageState 加载时机问题 → 调整 playwright.config.ts 的 dependencies
- 如果本地无法复现 → 等 CI 环境跑（方案 C）

**产出**：
- [ ] 失败模式分析报告（markdown）
- [ ] 修复方案选择 + 代码实施
- [ ] 全量跑通过截图

---

### 3. API 回归用例补齐（P0-3，对应已发现 bug）

**依赖**：P0-1 后端修复 S2 订阅金额穿透

**用例清单**：

| 用例名 | 断言 | 备注 |
|---|---|---|
| `test_purchase_below_min` | 400 拒绝 | `@pytest.mark.xfail` 标记 S2 |
| `test_purchase_equal_min` | 200 通过 | `amount=1` |
| `test_purchase_equal_max` | 200 通过 | `amount=999999.99` |
| `test_purchase_above_max` | 400 拒绝 | `amount=1000000` |
| `test_purchase_bot_paused` | 400 拒绝 | bot status ≠ Running |
| `test_redeem_exceeds_equity_rejected` | 400 拒绝 | `@pytest.mark.xfail` 标记 S2 |
| `test_purchase_wrong_user_id` | 403 identity_mismatch | `@pytest.mark.xfail` 标记 S3 |

**步骤**：
1. 在 `test_spartans_api.py` 补齐 7 条用例
2. 本地跑一遍确认 xfail 符合预期
3. commit message 加 `[AI-assisted]`

**产出**：
- [ ] `test_spartans_api.py` 新增 7 条
- [ ] pytest 输出（含 xfail 标记）

---

### 4. 跑一次完整 API E2E（P0-4，22 分钟）

```bash
cd /Users/haiifeng/obe-ai-testlab
source .venv/bin/activate
pytest projects/spartans/tests/api/test_spartans_api.py -v -m e2e
```

**目标**：验证 purchase → 等批次 → history=Finished → redeem → history=Finished 数据链

**结果（2026-08-18）**：✅ `5 passed in 661.71s (0:11:01)`

```
test_purchase_min_amount          PASSED
test_purchase_appears_in_history  PASSED
test_wait_for_batch               PASSED（等待 660s）
test_redeem_min_amount            PASSED
test_redeem_appears_in_history    PASSED
```

⚠️ **但这轮不算干净的验证**：运行期间我在同一账户上并发做了缺陷复验
（3 笔穿透订阅 + 1 笔清空权益的赎回），`test_redeem_appears_in_history`
断言"history 第一条是 Redeem"在有并发写入时恰好成立，不代表隔离条件下必然成立。
待专用资金账户就绪后需重跑一次干净的。

**产出**：
- [x] pytest 输出（见上）
- [x] 顺带发现 `1e-08` 订阅卡成 `Locked`，已补入 S2-A bug 文件

---

### 5. 评审 TEST_STRATEGY 5 条 TODO（P0-8，需产品/后端）

见 `SPARTANS_SUBSCRIBE_TEST_STRATEGY.md` 末尾：

| # | TODO | 对接人 | 结论 |
|---|---|---|---|
| 1 | 批次窗口内多次订阅合并 or 分记录？ | 产品 | 待确认 |
| 2 | Tier 参数更改通知机制？ | 产品 | 待确认 |
| 3 | metric/trace 命名规范（和 SRE 对齐） | 后端 | 待确认 |
| 4 | Private bot 白名单管理入口 | 产品 | 待确认 |
| 5 | 批次失败时的 Kafka topic / DB 状态 | 后端 | 待确认 |

**步骤**：
1. 整理 5 个问题到飞书文档/会议议程
2. 拉产品+后端过一遍
3. 结论回填到 `SPARTANS_SUBSCRIBE_TEST_STRATEGY.md`

**产出**：
- [ ] 会议纪要（或飞书文档链接）
- [ ] 策略文档更新 commit

---

### 6. 补 `shared/conventions/priority.md`（P0-7，10 分钟）

**参考**：`bug-severity.md` 格式

**结构**：
- P0–P3 判定标准
- 判定树（类似 severity 的表格）
- 典型场景示例

**产出**：
- [ ] `shared/conventions/priority.md` 文件

---

## 📊 今日完成度追踪

| 优先级 | 任务数 | 已完成 | 待开始 |
|---|---:|---:|---:|
| 上午 | 4 | 3 | 1（Happy Path 断言） |
| P0 下午 | 5 | 0 | 5 |
| **合计** | **9** | **3** | **6** |

---

## 🚫 今日暂不做（推到明天或下周）

- P1-1 ~ P1-15（本迭代任务）
- P2-1 ~ P2-20（下个迭代）
- 资金类测试数据隔离方案（P1 级）
- Playwright 选择器优化（`.nth(1)` 改语义定位）

---

## 📝 今日产出清单（预期）

- [x] `subscribe-frontend-validation.spec.ts` P0 修复（实测 6 passed）
- [ ] `subscribe-happy.spec.ts` P0 修复 — 待 codegen 校准"我的订阅"列表 DOM
- [x] `README.md` 认证方案文档统一
- [x] `SPARTANS_E2E_TEST_CASE_REVIEW_20260818.md` 更新（含勘误 1 条）
- [ ] Playwright 并发失效分析报告
- [ ] Playwright 并发失效修复代码
- [ ] `test_spartans_api.py` 新增 7 条用例
- [ ] API E2E 完整跑通报告
- [ ] TEST_STRATEGY 5 条 TODO 评审结论
- [ ] `shared/conventions/priority.md` 文件

---

## 🔔 风险提示

1. **Playwright 并发问题**可能需要等 CI 环境才能复现，本地排查可能无结论
2. **API E2E 22 分钟**依赖 Testnet 批次窗口（10min），如果窗口变更会超时
3. **TEST_STRATEGY 评审**依赖产品/后端有空，可能今天约不上

---

## 📅 明日预告（2026-08-19）

假设今日 P0 全部完成：

- P1-2：数据一致性对账测试（钱包-账户-份额-资金池-history 五路）
- P1-3：补 UI E2E（frontend-validation 已有，剩余 risk/i18n/mobile）
- P1-10 ~ P1-14：补文档/模板（命名规范、commit 规范、用例模板、bug 模板、CI 骨架）

# SPARTANS TODO — 2026-08-17 会话收尾

> 整理于 2026-08-17 晚，明天（2026-08-18）开工时直接按优先级从上往下执行。
> 完整排期见 [`2026-08-17-p0-p2.md`](2026-08-17-p0-p2.md)，本文件只列今天遗留的未竟项。

---

## P0：本周必做（阻塞上线）

### 1. Playwright 并发跑登录态失效 ← 今天最大技术债
- **现象**：全量跑（20 条用例串行）时，setup 之后 storageState 加载偶发失效，页面弹 auth-modal
- **单跑没问题**：subscribe-happy / risk-disclosure / i18n / logout 单独跑全绿
- **已排除**：session 单点顶掉（你没登录过 barton.c）、storageState 文件损坏、token 长度异常
- **待验**：投入 API 调用（invest/purchase）后前端触发 logout 或 JSESSIONID 被 server revoke
- **破局选项**（选一个）：
  - A. 每个 spec 的 `beforeEach` 里调 `loginViaUI` 重新登录（慢 6s × 用例数，绝对稳）
  - B. `beforeEach` 探测 `.auth-modal-overlay` 存在时自动重登（兼顾速度）
  - C. 等 CI 环境跑，排查是不是本机代理导致

### 2. 补 API 回归用例（对应已发现 bug）
- `test_purchase_below_min` — `amount=0.5` 应拒绝，当前穿透（S2），用 `@pytest.mark.xfail(strict=True)`
- `test_purchase_equal_min` — `amount=1` 应通过
- `test_purchase_equal_max` — `amount=999999.99` 应通过
- `test_purchase_above_max` — `amount=1000000` 应拒绝
- `test_purchase_bot_paused` — bot status ≠ Running 应拒绝
- `test_redeem_exceeds_equity_rejected` — `settleAmount=999999999` 应拒绝，当前静默截断（S2），用 xfail
- `test_purchase_wrong_user_id` — body.userId ≠ JWT 应 identity_mismatch，当前静默（S3），用 xfail

### 3. 跑一次完整 API E2E
```bash
cd /Users/haiifeng/obe-ai-testlab
source .venv/bin/activate
pytest projects/spartans/tests/api/test_spartans_api.py -v -m e2e
```
约 22 分钟，验证 purchase → 等批次 → history=Finished → redeem → history=Finished 数据链。

### 4. 评审 TEST_STRATEGY 5 条 TODO（找产品/后端）
见 `projects/spartans/docs/SPARTANS_SUBSCRIBE_TEST_STRATEGY.md` 末尾：
- 批次窗口内多次订阅合并 or 分记录？
- Tier 参数更改通知机制？
- metric/trace 命名规范（和 SRE 对齐）
- Private bot 白名单管理入口
- 批次失败时的 Kafka topic / DB 状态

### 5. 补 `shared/conventions/priority.md`
P0–P3 判定标准（10 分钟，对照 bug-severity.md 格式写）

---

## P1：本迭代（2026-08-18 → 2026-09-07）

| # | 任务 |
|---|---|
| P1-1 | 后端修复 S2 订阅金额穿透 + QA 回归 |
| P1-2 | 后端修复 S2 赎回静默截断 + QA 回归 |
| P1-3 | 后端修复 S3 userId 与 JWT 不匹配 + QA 回归 |
| P1-4 | 数据一致性对账测试（5 路：钱包-账户-份额-资金池-history） |
| P1-5 | 契约测试骨架 `automation/contracts/spartans.yaml` |
| P1-6 | 赎回模块 TEST_STRATEGY（`skill: test-strategy-generator`） |
| P1-7 | 分润 HWM 模块 TEST_STRATEGY |
| P1-8 | 取消订阅模块 TEST_STRATEGY |
| P1-9 | Runbook：`docs/SPARTANS_SUBSCRIBE_RUNBOOK.md` |
| P1-10 | 安全清单 7 项手工过一遍（越权/注入/限流/CSRF/重放） |
| P1-11 | 补 `shared/conventions/testcase-naming.md` |
| P1-12 | 补 `shared/conventions/commit-message.md` |
| P1-13 | 补 `ai-testlab/templates/testcase.yaml` 骨架 |
| P1-14 | 补 `ai-testlab/templates/bug.md` 骨架 |
| P1-15 | CI 骨架 `automation/ci/`（跑 API smoke + Playwright happy） |

---

## Playwright 遗留状态快照

```
当前通过（单跑稳定）        全量跑失败原因
──────────────────────     ──────────────────────────────
setup（登录抓 storage）     storageState 偶发不生效 → auth-modal
subscribe-happy             受上面影响，单跑过，全量挂
subscribe-risk-disclosure   受上面影响，单跑过，全量挂
i18n × 3（按钮存在）        稳定通过
i18n × 3（打开弹窗）        ERR_CONNECTION_CLOSED（网络抖动）
subscribe-logout            稳定通过（anon project）
login.spec                  稳定通过（anon project）
frontend-validation abc     稳定通过
frontend-validation 空/0/0.5  "UX 现状记录"断言，全量跑时弹窗没开出来导致失败
```

**快速验证命令**（明天开工先跑这两条确认基线）：
```bash
cd projects/spartans/tests/e2e
# 单跑 happy path
npx playwright test subscribe-happy --retries=0

# 单跑全部（如果 10 min 太长可以只跑前 3）
npx playwright test subscribe-happy subscribe-risk-disclosure subscribe-i18n --retries=0
```

---

## 已完成（今天）

- ✅ API 三层接口栈（obe_http.py + spartans client + SPARTANS_API.md）
- ✅ 9 条 API smoke 全绿
- ✅ 登录换 token 脚本（`scripts/spartans_login.py`，Testnet OTP 固定 123456）
- ✅ S1 验证脚本（`scripts/verify_bug_s1.py`）
- ✅ S1 降级为 S2（赎回静默截断，非越权）
- ✅ S3 越权定性（userId 与 JWT 不匹配，降级为设计缺陷）
- ✅ 3 份 bug 报告（S2 订阅穿透 / S2 赎回截断 / S3 userId 不匹配）
- ✅ bug-severity.md（S1–S4 分级标准 + 判定树 + SLA）
- ✅ dod.md（Definition of Done 上线闸）
- ✅ TEST_STRATEGY 模板 + test-strategy-generator skill
- ✅ SPARTANS_SUBSCRIBE_TEST_STRATEGY.md（首个完整策略示例）
- ✅ Roadmap P0–P2（`2026-08-17-p0-p2.md`）
- ✅ fixtures/tier_config.yaml（Tier 参数 + 批次窗口基线）
- ✅ Playwright 骨架（10 条稳定用例，storageState 方案，auth.setup.ts）
- ✅ USER2/USER3 token 已写入 .env.local
